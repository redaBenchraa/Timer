import 'package:flutter/widgets.dart';

class Entry {
  final int id;
  final Color color;
  final String title;
  final String subTitle;

  const Entry({this.id, this.color, this.title, this.subTitle});
}
